/** How many archive rows show before “Load more” (tune without touching component logic). */
export const ARCHIVE_INITIAL_PAGE_SIZE = 16;
/** Each click of “Load more” reveals this many additional rows (after search/filter). */
export const ARCHIVE_LOAD_MORE_SIZE = 16;
